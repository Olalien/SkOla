{
  "name": "olaskole",
  "version": "1.0.0",
  "description": "Interaktiv læringsplattform for ungdomsskolen",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
